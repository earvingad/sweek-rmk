name: Build RMK firmware
on:
  workflow_dispatch:
  push:
    paths:
      - "**"

jobs:
  build:
    uses: earvingad/sweek-rmk/.github/workflows/user_build.yml@main
    with:
      cargo_toml_path: "Cargo.toml"
      keyboard_toml_path: "keyboard.toml"
      vial_json_path: "vial.json"
